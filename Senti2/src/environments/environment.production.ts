/**
 * Producción: no incluyas claves reales en el repo.
 * Opciones: (1) Sustituir este archivo en CI con valores de env.
 * (2) Cargar en runtime desde window.__env (ej. script antes de index.html).
 */
export const environment = {
  production: true,
  apiUrl: '/api/v1',
  supabaseUrl: '',
  supabaseKey: '',
};
